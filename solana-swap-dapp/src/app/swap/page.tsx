// src/app/swap/page.tsx
import { SwapForm } from '@/app/components/SwapForm/SwapForm'; // 正しいパスを指定してください

export default function SwapPage() {
  return (
    <main >
      {/* 必要であればページタイトルなどを追加 */}
      <SwapForm />
    </main>
  );
}